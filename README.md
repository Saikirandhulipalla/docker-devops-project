# docker-devops-project
It's a project which runs a flask application  using multi-stage docker file and nginx reverse proxy which secures without exposing the IP by using user-defined docker network.
